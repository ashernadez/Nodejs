/// CommonJS  require module///
// Importa la función 'sum' desde el archivo './sum'
const sum = require('./sum')

// Imprime en la consola el resultado de llamar a la función 'sum' con los argumentos 1 y 2
console.log(sum(1, 2))